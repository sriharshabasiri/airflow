testing bashoperator for task3 - copying scripts
testing bashoperator for task3 - copying scripts


keytool -list -v -keystore truststore.jks -storepass truststorepassword | awk '/Valid from:/{start=$3} /Valid until:/{end=$4} /Alias name:/{alias=$3} /Alias name:/{if (systime() > mktime(end)) {print "Alias: " alias ", Valid from: " start ", Valid until: " end}}'
